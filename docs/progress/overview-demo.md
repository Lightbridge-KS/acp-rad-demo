---
summary: Tracker for the ACP-Rad Demo — slices 1–7 as checkboxes, Now/Next, deferred items, confirmed contracts.
read_when: Starting any session on this repo; before planning a slice; when landing work (tick boxes, add SHAs).
---

# ACP-Rad Demo — progress

Design: [`01-system-architecture`](../design/01-system-architecture.md) · [`02-surface-architecture`](../design/02-surface-architecture.md) · [`03-agentic-architecture`](../design/03-agentic-architecture.md) · [`04-skills`](../design/04-skills.md) · [`05-data-architecture`](../design/05-data-architecture.md) · [`06-report-parsing`](../design/06-report-parsing.md) · glossary [`CONTEXT.md`](../../CONTEXT.md). Sub-trackers: [`report-parsing`](./report-parsing.md) · [`skills-layered`](./skills-layered.md). Original draft (build order, 2026-08-29 decision ledger): [`archive/design/acp-rad-poc-spec.md`](../archive/design/acp-rad-poc-spec.md).

## Milestones

- [x] **1. Tracer bullet** (2026-08-29, `9d41220`) — repo bootstrap; editor mounts Quill with one fixture; browser ACP client ⇄ WS bridge ⇄ rad-agent (stdio); `initialize`/`session/new` carry `_meta.rad`; prompt streams into the sidebar. Verified: `just check` green; `just smoke` OK on Ollama `gpt-oss:20b` and hosted `gpt-5`; browser round-trip via Chrome; unknown `?agent=` closes with 4004.
- [x] **1b. Level 0 spike** (2026-08-29, `_playground/2026-08-29_level0-spike/NOTES.md`) — `claude-agent-acp` 0.70.0 over the same bridge (`?agent=claude`), two runs. Feasible for the demo. Never uses client `fs/*`; edits the real disk; inherits the host user's permission mode (`auto` ⇒ silent approval, run 1 edited via Bash with no diff/permission); with `session/set_mode: default` the Edit path yields `tool_call_update{diff}` → `request_permission{Deny, Allow Once, Always Allow}` → file on disk. Rules graduated to design §8 and *Confirmed contracts*.
- [x] **2. ReportStore** (2026-08-29, `d573f05`) — Delta⇄Markdown (label-line grammar, 72 tests incl. round-trip over 12 real files), virtual namespace + RO rules, `fs/read_text_file` served from live Quill; manifest at `session/new`; `AcpClientBackend` (ls/read/glob/grep over the client, writes refused). Verified: `just check` green; `just smoke` OK on Ollama and hosted `gpt-5` (read tool → `fs/read_text_file` → organ label); browser: bold labels render, hand-edited line is served live to the agent.
- [x] **3. Sign-off** (2026-08-29, `cf0d6d3`) — inline tracked changes in Quill (line-level hunks, ADR 0002; `ai-insert`/`ai-delete` overlays stripped from the buffer), per-hunk Insert / Insert as draft / Discard, permission answered from the decisions (clinical verbs on the wire via `PermissionRewritingClient`), grant read-through + `applied`/`partial` write outcome, `ai-draft` marks with per-line clear + Mark all reviewed, audit JSONL via the bridge, sidebar on `@assistant-ui/react` 0.15.17 (ADR 0001), `RadReportAgentServer` rename, demo start-state (impression blanked). Verified: `just check` green (85 + 24 + 23 tests); `just smoke` OK on Ollama and hosted (edit → permission → write → landed); browser scenario 1 with `gpt-5.6-terra`: overlay → typing while pending → Insert as draft → amber → per-line clear → Mark all reviewed → second proposal → Discard; audit trail on disk.
- [x] **4. Commands, snippets, priors** (2026-08-30, `196ce93` `9592402` `c949c5b` `ab03daf` `a8f08bb` `a3f569f` `a9e9d2d`) — one command registry behind `Commands ▾`, the in-report `/` menu and the composer `/` (Suggested · Editor · Skills); document commands `/template` (scenario 0, deterministic; option C tracked changes on a non-blank buffer; short-prelim fold-in) and `/short-prelim` (3b); snippet commands `/er-reviewed` · `/er-not-reviewed` · `/discuss-with-dr` (home-anchored); fixtures: blank CT brain + CXR and CT chest with priors (+ `?case=`); agent advertises `/impression` · `/compare` · `/proofread` via `available_commands_update` (scenario 2); cancel polish (scenario 5); vocabulary rename: `ai-draft` → unreviewed, UI verbs *Accept / Accept for review / Reject* on a *change* (code keeps `hunk`), INV-1 = human gate, `reportStatus` → `draft | preliminary | final` + `shortPrelim`. Spec: design 02 §2.2, §4–5; skills in design 04. Reject (scenario 4) already lands in slice 3. Verified: `just check` green (87 + 74 + 30 tests); `just smoke` OK on hosted `gpt-5.6-terra` incl. stage 2 (skills advertised, `/compare` lands both prior dates); browser with `gpt-5.6-terra`: scenario 0 (`/temp` → instant skeleton, dose filled, ⌘Z), 3b (SP → typed critical finding → fold-in as tracked changes → Accept all → `draft`, `short_prelim.folded`), ER-marker toggle + idempotent discussed-with, `Commands ▾`, composer `/`, scenario 2 (`/compare` → two changes → Accept all for review → amber), scenario 5 (Stop → *stopped*, `session.cancel`). Not exercised live: `/proofread`, the Level 0 Skills-hidden rule (`?agent=claude`).
  **Plan (approved 2026-08-30; build order, one commit each, `just check` green at each):**
  - [x] 4.1 (`196ce93`) vocabulary rename first (so all new code is born with the right words): `ai-draft`→`ai-unreviewed` marks/blot/CSS, pills *Accept / Accept for review / Reject*, header counters, `draft.cleared`→`review.cleared`, `zReportStatus = draft|preliminary|final` + `shortPrelim`, option display names (ids unchanged) in editor and `permissions.py`.
  - [x] 4.2 (`9592402`) pure command functions in `apps/editor/src/commands/` (house policy stays out of `packages/acp-rad`): template instantiation (`[Male]`/`[female]` by sex, unknown sex keeps both lines *with* tokens; dose blanks filled by pattern), SP document, fold-in (via `splitSections` impression range; SP-ness is the `shortPrelim` flag, never sniffed), ER-marker toggle, idempotent discussed-with; unit-tested.
  - [x] 4.3 (`c949c5b`, browser fixes `a8f08bb`) registry + three surfaces + **local proposals**: `Proposal.origin: agent|local` — local = no grant (a grant would serve the pre-command buffer to the agent for 60 s), no waiter, `accept_edit` coerced to `accept`, excluded from `matchPending` and `cancelAll`; Accept/Reject pills only. `Command.run` is Quill-free and returns an effect. In-report `/`: capture-phase keydown on the editor container (Quill bails on `defaultPrevented`), one-line `slashAt` detection, items `onMouseDown.preventDefault`. Composer `/`: assistant-ui 0.15.17 `Unstable_TriggerPopover` + `unstable_useSlashCommandAdapter({removeOnExecute:true})`. Live `final` lock moves into `ReportStore.assertWritable` via a `reportStatus()` getter. Snippets on a non-blank buffer are instant inserts (💡 confirmed in plan); only document commands take option C.
  - [x] 4.4 (`ab03daf`) fixtures `ct-brain-er-blank` (meta only), `cxr-pa-prior`, `ct-chest-er-nodule-prior` (2 priors: CT chest, CT whole abdomen) with hand-written `priors/index.md` (04 §4 format; loader excludes it from priors; store serves `priorsIndex` verbatim), `meta.json.study.date`, `?case=`. KS reviews the synthetic reports.
  - [x] 4.5 (`a3f569f`) agent skills: `prompts/skills/<name>.md` (2-key frontmatter, hand-parsed — no pyyaml), `available_commands_update` scheduled after `session/new` (guard: tests construct the server with no `_conn`), `prompt()` expansion; `/compare` added to `just smoke` on the CT chest case.
  - [x] 4.6 (`a9e9d2d`) *stopped* marker (rendered from `state.messages`, no assistant-ui API), docs/tracker sync (02 loses *planned* tags; 02 §4.2 diagram corrected — LCS keeps the SP lines, so the fold-in renders as two insert hunks, nothing struck).
- [x] **5. QA** (2026-08-30, `7468fc6` `33b70bb` `b457355` `38bb36d` `d5166e0`) — `raise_flag(kind, summary, locations)` tool → `_rad/flag` request → flag card + flagged line marked → acknowledged → audit; kinds `discrepancy` · `omission` · `unsupported` · `critical_uncommunicated` closed by the schema on both sides; agent flips `flags: true` (Level 2); `/qa` advertised only to a client that negotiated flags; new fixture `ct-wa-er-stone`. Scenario 3. Spec: design 04 §3.5. Verified: `just check` green (90 + 81 + 40 tests); `just smoke` OK on hosted `gpt-5.6-terra` incl. stage 3 (stone → exactly `["discrepancy"]` at `sections/impression.md` line 2, no writes; brain → `["critical_uncommunicated"]`); browser with `gpt-5.6-terra` (Chrome, Browser 1): header **L2**; `/qa` on the stone study → tool card `flag raise_flag` → card `discrepancy · sections/impression.md · line 2` → the IMPRESSION line marked → typing on it neither spreads nor clears the mark → **Acknowledge** clears mark, card and the header pill → Audit `flag.raised … f1 → discrepancy`, `flag.acknowledged f1`, on disk in `audit/ACC0000031.jsonl`; stroke study `/impression` → Accept all for review → `/qa` → one `critical_uncommunicated · sections/findings.md · line 10` (the Vascular-system line marked, amber items untouched). Not exercised live: Stop mid-`/qa` (flags are never cancelled by construction), deleting a marked line (unit-tested).
  **Plan (approved 2026-08-30, `~/.claude/plans/inherited-dreaming-lake.md`; build order, one commit each, `just check` green at each). Rulings (KS): (1) `_rad/flag` is answered `acknowledged` by the **Client on receipt** — once the `FlagStore` holds it and the line is marked; the radiologist's click is local, audited `flag.acknowledged` (turn never blocks; the slice-6 gate counts at `end_turn`; nothing in flight on Stop); proposal §8.2's human `acknowledged | dismissed` is dropped. (2) Discrepancy fixture = new CT whole-abdomen study `ct-wa-er-stone` (right UPJ stone in FINDINGS, "left" in IMPRESSION); KS reviews. (3) "never edits" is prompt + schema-enforced kinds only; editor enforcement waits for the slice-6 gate.**
  - [x] 5.1 (`7468fc6`) schema (`zFlagKind` · `zFlagParams` · `zFlagResponse` · `FLAG_METHOD`; `locations[].line` = 1-based line of the file *as the agent read it*; `flagId` on the audit record), `FlagStore` (`report/flags.ts`, never cancelled by the agent), fourth mark `ai-flag` (in `OVERLAY_FORMATS` and `NO_MARKS`, **not** in `withoutOverlayAttrs` — a rejected proposal must not un-flag a struck line), `flagLineOps` (ordinal walk mirroring `canonicalLines`, then text check, text fallback never on blank/label lines), `clearFlagOps`, `flagLineIndex` (`runs.some`).
  - [x] 5.2 (`33b70bb`) wire `onRequest("_rad/flag", zFlagParams)` → `App` (`FlagStore` per fixture; target text from `peekGrant(path)?.baseText ?? store.read(path)`; audit `flag.raised` / `flag.acknowledged`; header "n flags"); sidebar flag-card strip in the Chat tab with **Acknowledge** (the one decision the sidebar owns); scroll-to on click; `AuditPanel` shows `flagId`.
  - [x] 5.3 (`b457355`) agent: `flags.py` `raise_flag` (`@tool` + pydantic `Literal` kinds; `await conn.ext_method("rad/flag", …)`; returns text, never raises), `prompts/skills/qa.md` (`requires: flags`), `advertise(skills, client_caps)` filtering, `AGENT_RAD_CAPS.flags = True` (Level 2), tool passed only when the client negotiated flags.
  - [x] 5.4 (`38bb36d`) fixture `ct-wa-er-stone` (+ `?case=`), smoke stage 3 (`/qa` → `discrepancy`, no writes) and 3b (brain: `critical_uncommunicated`), `some`-based checks, live on hosted `gpt-5.6-terra`.
  - [x] 5.5 browser: scenario 3 + stroke `/impression` → `/qa`; L2 in the header; mark survives typing and a rejected proposal, goes with a deleted line; Stop leaves flags.
  - [x] 5.6 (`d5166e0`) docs (01 §6.4/§8, 02 §2.1, 03 §5/§8, 04 §3.5/§6, protocol §3.3/§5.12/§7, data §2/§3.2/§5/§8, `CONTEXT.md` Flag) + tracker tick.
- [x] **6. Demo polish** (2026-08-30, `720d1bd` `a87661d` `0a29566` `5a71d62` `585e6f9` `4b4beb7`) — worklist select (5 cases, inline discard confirm, `?case=` sync), role toggle, status pill with Prelim / Sign off behind the **QA gate** (deterministic: empty · pending · unreviewed · blanks; then advisory `/qa` counted on the `FlagStore`; `qa.refused` / `qa.passed` / `qa.overridden {flagIds}` / `qa.skipped {reason}` / `status.changed`; SP exempt — design 04 §3.5), final lock (Quill disabled, editor commands hidden, agent writes refused before any proposal), `/qa`-turn write refusal, provider switch through plain ACP (`session/set_config_option`), audit panel filters. Verified: `just check` green (91 + 105 + 46 tests); `just smoke` OK on hosted `gpt-5.6-terra` incl. stage 4; browser: every gate outcome (refused, passed, overridden, cancelled, agent absent), the Prelim → Sign off chain, the final lock on both paths, the worklist confirm, a live model switch to `anthropic:claude-sonnet-5`.
  **Plan (approved 2026-08-30, `~/.claude/plans/goofy-frolicking-brooks.md`; build order, one commit each, `just check` green at each). Rulings (KS): (1) **Provider switch is ACP-native** — `session/set_config_option` on the `model` select deepagents-acp builds from `models=` (0.0.11; TS SDK 1.4.0 has `setSessionConfigOption`); reverses the bridge-side `?model=` idea on evidence; the bridge stays a pipe. (2) **Roles = header toggle** resident | attending, display-only: resident → Prelim (draft → preliminary), attending → Sign off only (draft | preliminary → final); stamps `actor.role`. (3) **`/qa`-turn write refusal applies to any `/qa` turn**, hand-typed or gate-sent; editor-enforced, audited `permission.refused` / `fs.write.refused {outcome: qa}`. (4) **Worklist switch** with pending / unreviewed / running → inline confirm, then discard; nothing persisted across cases. (5) `/sign-off` is not a command — pill actions only; agent-gate timeout 90 s; `qa.overridden` carries `flagIds`; the agent's frozen `reportStatus` stays v0.2.**
  - [x] 6.1 (`720d1bd`) schema (`flagIds`, `attending`), `report/lifecycle.ts` (transitions × roles; deterministic gate: empty · pending · unreviewed · blank `_{2,}` — templates write `__ hours` too), `report/qaGate.ts` reducer with declarative effects (`prompt · cancel · transition · audit`; late `turn_end` is a no-op), `AuditLog.setActor` (own field — `bind` replaces `ctx`).
  - [x] 6.2 (`a87661d`) `App.tsx` → shell (fixture, role, worklist select, `?case=` sync) + `Workspace` keyed per case; status pill actions; gate panel (refused / running / flagged / unavailable); role toggle → `audit.setActor`; `AgentPort.prompt` returns the stop reason; one turn at a time. Browser: blank case → Prelim refused *empty* → `/template` → refused *6 blanks left*; stone → Sign off → `/qa` → 1 `discrepancy` → *Sign off anyway* → `final`; JSONL: `command.qa → gate · flag.raised f1 · qa.overridden {flagIds:[f1]} · status.changed → final`, `actor.role = attending`.
  - [x] 6.3 (`0a29566`) final lock (`quill.enable(false)` via `ReportEditor.readOnly`, editor commands hidden incl. *Suggested*, `runCommand` guard) + `refuseReason(): final | qa | null` in `connection.ts` — no proposal rendered (`onUpdate` guard), `request_permission` answered with the agent's `reject_once` option, `fs/write` `-32003`; `handle.prompt` refuses a second concurrent turn; the composer ignores Send while running. Browser at `final`: typing dropped, `Commands ▾` = skills only, `/impression` → no tracked change, agent says "edit rejected by the editor", JSONL `permission.refused {outcome: final}`. Nit: the refused edit's tool card stays `requires-action` (no proposal to close it).
  - [x] 6.4 worklist confirm (inline banner from `Workspace.dirty()` — pending hunks · unreviewed · open flags · running turn; *Switch* cancels best-effort and remounts; *Stay*) + `?case=` sync. Fixed on the way: a write that already matches the buffer (idempotent `/template`) used to leave a zero-hunk proposal `pending` forever — `add` now registers it `applied`. Browser: blank → `/template` → `/short-prelim` (local proposal) → pick CXR → banner "discards 1 pending change" → Switch → fresh session, URL synced.
  - [x] 6.5 (`585e6f9`) provider switch, plain ACP: `RAD_MODELS` → `AgentServerACP(models=…)` (always ≥ 1 entry; `build_agent` uses `context.model`), `session/new.configOptions` → sidebar header `<select>` (disabled while a turn runs) → `session/set_config_option` (audit `session.config → model=<spec>`), `config_option_update` refreshes the header; smoke stage 4 (`modelOptionAdvertised · setModelAccepted · promptAfterSwitch`); runbook. Browser with `RAD_MODELS=openai:gpt-5.6-terra,anthropic:claude-sonnet-5` (keys from `.env`): switch → "PONG - I am Claude, a large language model made by Anthropic."
  - [x] 6.6 audit panel: family filter chips (`fs · permission · proposal · hunk · flag · qa · status · command · session · review`, only families present), actor role column, `flagIds`; connect failure reads "cannot reach the bridge at …" instead of `[object Event]`. Browser scenarios: (a) stone → `/proofread` fixes the laterality → Accept all for review → Mark all reviewed → Sign off → `/qa` "no flags" → **`qa.passed` → final**; (b) blank case refused (*empty*, then 6 blanks after `/template`); (c) stone override (`qa.overridden {flagIds:[f1]}`); (d) stroke → Sign off → **Stop** mid-`/qa` (two flags had already landed) → "QA unavailable — the check was stopped" → Sign off anyway → `session.cancel · qa.skipped{cancelled} · status.changed final`; (e) bridge down → Sign off → "the agent is not connected" → anyway → final; resident Prelim → `preliminary` → attending Sign off → `final` with no agent. Known: with no session the audit records stay queued in `AuditLog.pending` (no sink) — the Audit tab shows 0 and nothing is persisted; a flag raised before Stop stays in the strip but the gate reports *cancelled*, not *flagged* (by design: the turn did not finish).
  - [x] 6.7 (`3112789`) docs + tracker sync: 02 §2.1 (status pill, role toggle, model select, worklist rows built; §5.2 roles amended — attending has no Prelim; §8 `/sign-off` decided), 04 §3.5 (gate built; `flagIds`; counted on the `FlagStore`; editor-side "never edits"), 01 §6.4/§7/§10, data §5/§6.2/§7/§8 (status → `Workspace`; model choice row; event catalogue), protocol table (`set_config_option` used), glossary QA gate, runbook (worklist; `.env`), tracker Now/Next, Deferred, *Confirmed contracts → Slice 6*.
- [x] **Deployment packaging** (2026-08-30) — one multi-target Dockerfile builds the static editor and the Node 26 + Python 3.13 bridge/rad-agent runtime; Compose serves one browser origin through nginx (`/` editor, `/acp` WebSocket proxy), persists audit JSONL, injects model configuration from `.env`, and supports localhost or Tailscale-only demo hosting. Level 0 agents and public-Internet hardening are out of scope. Verified: `just check` green (91 + 107 + 46 tests); Compose config + both images + health checks green; containerized live tracer `SMOKE OK`; Chrome through nginx `/acp`: L2 agent ready → `/impression` → one gated change → Accept → audit volume, with no console errors.
- [x] **Vercel demo hosting** (approved 2026-08-30; amended by KS to anonymous access before rollout) — convert the frontend-only deployment into one same-origin Vercel Services project: Vite editor + containerized bridge/rad-agent; no app-level authentication; global 10-agent admission and 7-day audit retention through managed Redis; Vercel AI Gateway `openai/gpt-5.6-terra` with a dedicated $10/day Production budget; Production-only live LLM; manual reconnect after WebSocket recycling. Public access remains synthetic-only and fail-closed when admission or model configuration is unavailable.
  - [x] Application slice: anonymous origin-checked WebSocket admission, Redis leases/audit retention, reconnect-preserving editor UX, Gateway credential precedence, same-origin Services manifest, Compose compatibility, ADR and runbooks. The initially implemented passphrase/session path was removed completely after the plan amendment.
  - [x] Dry gates: `just check`, editor production build, bridge container build, Compose health/same-origin WebSocket checks, and `vercel build --target preview` (editor + Linux container) green.
  - [x] Vercel project baseline: repository root, Services preset, Fluid compute, Production/Preview environment policy, separate signing secrets, and dedicated Production Gateway key with a $10 daily budget.
  - [x] Hosted Redis: Marketplace terms accepted by KS; `upstash-kv-citron-planet` provisioned and attached to Production, Preview, and Development.
  - [x] Hosted rollout: superseded authentication variables removed; Preview and Production deployed. Production live smoke green against `wss://acp-rad-demo-editor.vercel.app/acp` (`BRIDGE_URL`/`BRIDGE_ORIGIN` retarget `scripts/smoke.ts`) — all 22 checks, `SMOKE OK`, exit 0: rad caps, gated `/impression`, `/compare` across both priors, `/qa` raising `discrepancy` and `critical_uncommunicated` with no writes, and the model select.
  - [x] First production model call failed `403 RestrictedModelsError` — the team was on the AI Gateway **free tier**, which serves only a subset of the catalogue; the $10/day key caps spend but grants no credits. KS purchased credits; no code or configuration change was needed. `GET /v1/models` lists the full catalogue regardless of entitlement, so it cannot verify access — the runbook now verifies by calling the model.
- [x] **Layered skills** (2026-08-31, `f0d7fa7` `68e1f03` `d3af0f0` `ec48c5a` `c3e4cce`) — a skill stops being a whole-prompt regex substitution inside the agent's package and becomes an **Agent-Skills folder resolved across builtin → house → personal** (ADR 0004). The institution and the individual radiologist can now author skills; `/qa` is *sealed*, so later layers append checks and can never replace the base. Invocation is a **mention** — `/name` anywhere in a sentence, carried as a plain ACP `resource_link` and resolved before the model runs. The audit gains context provenance (model in force, skill, client-served layers), and `initialize._meta.rad.skillContract` publishes the surface an outside skill author may rely on. Fixes two live bugs found on the way: `FilesystemPermission` had never been enforced agent-side, and `isQaPrompt` was start-anchored so a mid-sentence `/qa` would not have tripped the write lock. Resolves the `skills=` transport 💡 in design 03 §9 and unblocks 04 §6's reference-material tier. Verified: `just check` green (186 + 10 + 136 TS, 89 py — 47 → 89). Not yet re-run: `just smoke`, and a browser pass. Detail: [`skills-layered.md`](./skills-layered.md).
- [ ] **7. (stretch) Level 0 in the demo** (~1 day) — target **`claude-agent-acp` first, `gemini --experimental-acp` second** (design §2/§8). Bridge: materialize the namespace to an on-disk mirror + file-watch (`_bridge/file_changed`); write `<mirror>/.claude/settings.json` denying Bash/Write/WebFetch so edits go through `Edit`. Editor: already pins `session/set_mode: default` and filters `allow_always` (slice 3); still needs to ignore L0 `available_commands_update` and apply grant matching to the file-watch event instead of `fs/write_text_file`.
- [x] **Report parsing (tolerant section labels)** (2026-08-31, `66968ea` `f59a1d1` `e290a57` `2b8a809`) — a pasted report that is not in house grammar parsed to *zero* sections, so `/impression` could not read or write `sections/*.md` while `ls` still listed them. Port of the keyword model from KS's Python `radreportparser`, line-anchored. P1–P3 landed: `labels.ts` (tolerant, line-anchored, configurable), a manifest that cannot go stale, absent sections created through the gate, `/normalize`. Verified: `just check` green; the fixture sweep proves the change additive; browser run on `ct-neck-tb-lymph` with `openai:gpt-5.6-terra` — `/impression` reads both section files and proposes in 4 tool calls (was 9 with an "unable to access" message), `/normalize` converts the report in one approved diff. Not yet exercised live: `just smoke` since these changes. Design [`06-report-parsing`](../design/06-report-parsing.md) · detail [`report-parsing.md`](./report-parsing.md).

## Now / Next

- **Now:** the anonymous public demo is live at `https://acp-rad-demo-editor.vercel.app` and verified by the production smoke. Outstanding before wider sharing: KS reviews the four synthetic cases for clinical plausibility.
- **Next:** layered skills — skills become Agent-Skills folders resolved across builtin → house → personal, mentioned in-sentence, and recorded in the audit trail ([`skills-layered.md`](./skills-layered.md), plan approved 2026-08-31). Slice 7 (Level 0 in the demo) and the Zed-against-`rad-agent` run remain **deferred with no schedule** (KS, 2026-08-30).

## Deferred

- ~~assistant-ui as the sidebar frontend~~ — **resolved 2026-08-29: ADOPT-PARTIAL** (KS). See `docs/adr/0001-sidebar-on-assistant-ui.md`; spike record `_playground/2026-08-29_assistant-ui-spike/NOTES.md`. Lands in slice 3.
- ~~Rename `RadAgent*` → `RadReportAgent*`~~ — done in slice 3 (`RadReportAgentServer`, agent name `rad-report-agent`; package/CLI `rad-agent` kept).
- **Wireframe taste adjustments** (KS, 2026-08-29, applied to the canvas): no action bar — commands live behind a `Commands ▾` menu in the editor toolbar and a Notion-style in-editor `/` menu (Suggested · Snippets = instant editor inserts · Skills = agent proposals); the sidebar composer's `/` opens the same list. → slice 4.
- ~~Fixture `ct-brain-er-stroke` carries a filled IMPRESSION but scenario 1 needs an empty one~~ — resolved in slice 3 by a start-state rule: `meta.json.demo.start = "impression_empty"` blanks the items at load; the file stays the complete, reviewed case.
- Revise `docs/ideas/acp-rad-protocol-proposal.md` to match the design: §4.2 grammar → label-lines; drop §8.3 `section_patch`; §5 `reportStatus` enum; companion line "Flutter Quill" → QuillJS; `ramaai-dev` → `radrama-ai`.
- Canonical grammar v0.1 does not escape literal `*`/`_` (reports don't use them). Revisit only if a real report needs it.
- Local `gpt-oss:20b` occasionally answers without calling `read_file`; the smoke prompt now says "use your read_file tool". Hosted models are deterministic here.
- `session/load` resume. ~~Model dropdown via `session/set_config_option`~~ — landed in slice 6.
- After a refused agent edit (`final` / `/qa` turn) the tool card in the thread stays `requires-action` — no proposal exists to close it; the audit tells the story. Cosmetic.
- A flag raised *before* Stop during the gate turn stays in the strip, but the gate reports *cancelled*, not *flagged* — by design (the turn did not finish); revisit if it confuses in the demo.
- **Model-visible partial outcome** (v0.2): deepagents' `EditResult` cannot carry `_meta`, so after a partial accept the tool result still says "replaced 1 instance"; the agent learns the truth only by re-reading (the prompt says so). Fix candidate: a `_rad/` notification or an `edit_file` result annotation.
- Word-level hunks (ADR 0002 alternative) if whole-line strike/insert proves noisy for one-word edits.
- KS reviews the four synthetic cases (`ct-brain-er-blank`, `cxr-pa-prior`, `ct-chest-er-nodule-prior`, and slice 5's `ct-wa-er-stone` with its planted right/left discrepancy) for clinical plausibility — committed unreviewed.
- ~~Editor-side "never edits during `/qa`" enforcement~~ — landed in slice 6 (`refuseReason()`; any `/qa` turn, hand-typed or gate-sent).
- Zed (a foreign ACP client) against `rad-agent` unchanged — the M = 2 half of the M×N argument (protocol doc §9); **deferred, no schedule** (KS, 2026-08-30), together with slice 7.
- At browser zoom ≠ 100 % the whole page scrolls when a command moves the caret (Quill's `scrollSelectionIntoView` reaches `html`); the editor pane should own the scroll.
- The composer's assistant-ui trigger popover is an `Unstable_*` API pinned at 0.15.17; an upgrade must re-check `Sidebar.tsx`'s `Composer`.
- Dev-only nit: React StrictMode's double effect opens two ACP sessions per load, so `audit/*.jsonl` gets two `session.new` records in dev; production builds spawn once.

## Confirmed contracts

- **`/proofread`'s wording pass is an error test, not an improvement test (KS, 2026-08-31).** Measured on `ct-neck-tb-lymph`, same pristine fixture both runs. *Open form* ("fix spelling, grammar, …"): 7 pills, **11 lines rewritten**, about half preference — `shows`→`demonstrates`, `size up to`→`measuring up to`, `The rest of`→`The remaining`, whole-sentence restructures — beside real fixes. *Error test* (change a line only where it is wrong; synonym swaps, preposition changes and restructuring a grammatical sentence explicitly out): **6 lines of wording changes**, and their character shifted to articles, hyphens and spellings (`Hypopnuematization`→`Hypopneumatization`, `contrast enhanced`→`contrast-enhanced`, `The rest of oral cavity`→`The rest of the oral cavity`); two preference edits survived. **The headline pill count did not move (7 → 7)** because the freed budget was taken by house-grammar normalization: 11 further lines changed only `**LABEL**:` → `**LABEL:**`, which is `/normalize`'s job. **Cut and measured:** house grammar is out of step 1 — the wrapper belongs to `/normalize`, and `/proofread` is told so explicitly (`**LABEL**:` and `**LABEL:**` are both acceptable to it). Same fixture, same protocol: **7 pills / 17 lines → 3 pills / 6 lines**, with **0 label-wrapper changes** (was 11). What survives is almost all real: `contrast enhanced`→`contrast-enhanced`, `slice thickness`→`thicknesses`, `60 ml`→`60 mL`, `Hypopnuematization`→`Hypopneumatization` ×2, `The rest of visualized`→`The rest of the visualized`, and a run-on fixed in HISTORY with *Known case of TB lymph node* intact. **Two preference edits still leak** — `size up to`→`measuring up to` in both node bullets — the same synonym swap that survived all three prompt versions despite being named as forbidden. Prompt rules move the distribution; they do not enforce it. The three cuts together: 11 wording lines → 6, label churn 11 → 0, pills 7 → 3.
- **The HISTORY is not the radiologist's prose (KS, 2026-08-31).** Tried the `/qa` widening's analogue on `/proofread`'s consistency pass and it was **not needed**: `/proofread` already reads `report.md`, and its consistency list is a *closed enumeration* (laterality, size, count, lobe/segment, technique vs title) that never included etiology — it left the history-sourced TB diagnosis untouched, and its only IMPRESSION edit was a legitimate certainty alignment (`Chronic` → `Probable chronic`, matching FINDINGS' *probably*). `/qa` needed the widening because `unsupported` is *open-ended* ("no basis in FINDINGS"). **Patch where the rule is open-ended, not where it is enumerated** — the exception clause was reverted, since it teaches the model that impression items are exempt from the check it exists to make. What the runs did surface: `/proofread` twice rewrote the HISTORY, *"Known case of TB lymph node at right neck"* → *"Known right neck tuberculous lymphadenitis"*, promoting a descriptive phrase to a diagnosis. That closes a loop now that `/impression` derives etiology **from** the HISTORY: a proofreader that strengthens the history strengthens the next impression's basis without the evidence being re-examined. Step 1 now protects the history the way it protected findings; verified — the same line came back as *"Known case of TB lymph node at right neck on HRZE**,** presenting with new left neck mass**,** was sent for evaluation."*, two commas and nothing else.
- **`/impression` reads the HISTORY (KS, 2026-08-31).** On `ct-neck-tb-lymph` the findings (matted, necrotic, bilateral cervical and supraclavicular nodes) fit tuberculous lymphadenitis and nodal metastases equally; the discriminator is only in HISTORY, and reading FINDINGS alone the agent proposed *metastases*. Three consequences, all now in `prompts/skills/impression.md` and design 04 §3.1: (a) **history is read first** — the indication is why the study exists; (b) the anchoring risk it introduces is carried by an explicit precedence clause — *the findings are the evidence; the history only decides between diagnoses the findings already support*, never adds one, never overrides the images, never becomes an item; (c) a **discordance clause** — when the findings sit oddly with the known history (new disease under treatment) the item says so instead of assuming the known diagnosis. This forces a matching widening of `/qa`'s `unsupported`, which judged an item against FINDINGS alone and would have flagged its sibling's correct output: an etiology the history supplies is *supported* while the findings stay compatible with it. Verified in the browser (`openai:gpt-5.6-terra`): trace `ls → history.md → findings.md → impression.md → edit`, and the item became *"most likely tuberculous lymphadenitis in this patient with known right cervical tuberculous lymphadenitis; correlate for treatment response/progression"*.
- **Slice 6:** (a) **The gate is a reducer with declarative effects** (`qaGate.ts`): `start · turn_end · timeout · review · override · dismiss` → `prompt("/qa") · cancel · transition · audit`; the Workspace executes effects and feeds `turn_end` from `AgentPort.prompt`'s resolved stop reason. Flags "raised during the turn" = `flags.list().slice(countBefore)` — never compare `f{n}` ids as strings. Late or foreign `turn_end`s are no-ops. (b) **Refusal precedes proposal**: `onUpdate` turns a `tool_call` diff into a proposal *before* `request_permission` arrives, so a lock must be checked there too, not only in the handlers; the one predicate is `refuseReason()`. A refused permission is answered with the agent's *reject* option (kind `reject_once`), never `cancelled`. (c) **deepagents-acp already does model switching**: `AgentServerACP(models=[{value, name}])` → `session/new.configOptions` `model` select; `session/set_config_option` validates, stores `_session_models[session]`, calls `_reset_agent` → the factory gets `context.model`; the graph (and its `MemorySaver`) is rebuilt — a switch mid-turn would swap the agent under the running prompt, so the select is disabled while running. TS SDK 1.4.0: `acp.methods.agent.session.setConfigOption`, responses are not zod-parsed so `configOptions` survives; narrow `type === "select"` before reading `currentValue`. (d) **A zero-hunk write must not pend**: `ProposalStore.add` registers it `applied` (an idempotent `/template` used to leave a phantom pending proposal). (e) **`AuditLog.setActor` lives in its own field** — `bind` replaces `ctx` wholesale; queued records carry the actor they were made under. (f) **Template blanks are `__` as well as `____`** (`__ hours`); `E_V_M_` has single underscores — match `_{2,}`. `ct-brain-er-blank` loads as `"\n"`, so the gate needs an *empty* blocker. (g) **Per-case state resets by remount**: `App` (shell: fixture, role) + `Workspace key={fixture.id}`; the shell injects `headerStart` / `banner` and reads `dirty()` / `cancel()` through a ref; never `window.confirm` (it blocks the Chrome tool and the page). (h) Ops: keys in a root `.env` (`.env.example`), `RAD_MODELS` for the switch; `lb key run` is personal tooling and stays out of the repo docs. Chrome tool: screenshot scale changes between frames — click by `find` refs, drive `<select>`s with `form_input`.
- **Slice 5:** (a) **`_rad/flag` is acknowledged by the Client on receipt** (KS): the store holds the flag and the line is marked before the response leaves; the radiologist's Acknowledge is local (`flag.acknowledged`); nothing is in flight on Stop; the slice-6 gate counts `FlagStore.open()` at `end_turn`. (b) **Python SDK outgoing extension requests** go through `AgentSideConnection.ext_method("rad/flag", params)` — there is no `send_request` on it; `ext_method` prepends the `_`, returns the raw result dict, raises `RequestError` (error response) / `ConnectionError` (dropped pipe). (c) **A tool that raises escapes the LangGraph loop and kills the `session/prompt` handler** — profile tools return every failure as text (like `backend.py`); a pydantic validation error on the args (e.g. a fifth kind) becomes an error `ToolMessage` the model reads — proven through a real `ToolNode` in a compiled graph (a bare `ToolNode.ainvoke` needs the runtime and fails). (d) **deepagents-acp streams a custom tool** as `tool_call{kind: "other", title: <tool name>, rawInput}` and its returned string as `tool_call_update.content`; the sidebar labels it from `title`, not `kind`. (e) **Line anchoring**: `locations[].line` counts the file *as the agent read it* — served line N = `canonicalLines(file)[N-1]`, and while a grant is open that file is `grant.baseText`; the editor walks the buffer's section range like `canonicalLines` (insert lines skipped, blank runs collapsed) to the ordinal, verifies the text, falls back to a text match never on a blank or label line. (f) **`ai-flag` stays out of `withoutOverlayAttrs`** (which `decideHunkOps`'s reject path uses on struck lines) and is looked up with `runs.some` (typing at a line start strips the first run's marks); it is in `NO_MARKS` and `OVERLAY_FORMATS`; `deltaToMarkdown` ignores it anyway. (g) **Skills with a capability dependency** declare `requires: <cap>` in their frontmatter; `advertise(skills, client_caps)` hides them and the tool is passed to `create_deep_agent` only when the client negotiated the capability.
- **Slice 4:** (a) **Local proposals** (editor commands on a non-blank buffer) carry `origin: "local"`: no grant — a grant would make `fs/read_text_file` serve the pre-command buffer for 60 s —, no permission waiter, `accept_edit` coerced to `accept`, excluded from `matchPending` and from `cancelAll` (Stop/close), state → `applied` on decision. (b) deepagents-acp batches **parallel `edit_file` calls under one interrupt**: two `session/request_permission` arrive with the *same* `toolCallId` — correlate by `rawInput`. A proposal the radiologist decides *before* its request arrives stays `decided` with no `options` and is answered (grant included) when the request comes; `matchPending` includes such proposals. (c) **Quill:** never call `quill.getSelection()` from render or a tick-driven effect — it runs `quill.update()`, which can emit a text-change and chain React commits ("Maximum update depth exceeded"). Track the caret from `selection-change` (which Quill reports *before* the model catches up with a keystroke → derive after a macrotask) and from text-change deltas (`caretAfter` for user changes, `transformRange` for api changes); coalesce `setTick` per animation frame. Quill's keyboard module bails on `defaultPrevented`, so a capture-phase `keydown` on the editor container is enough to own ↑↓⏎⇥⎋ while a menu is open. (d) **Skills:** `RadReportAgentServer` sends `available_commands_update` as a task right after the `session/new` response and expands `/name [arg]` in `prompt()`; the editor's sidebar store keeps `{name, description, hint}`; skills are hidden when `level === 0`. (e) **Menus:** the composer's `/` lists skills only — editor commands are not chat (KS, 2026-08-30, amends "the same list on three surfaces"); a typed query collapses the groups into one ranked *Matches* list (exact id · id prefix · id substring · description) so Enter never picks a snippet whose description mentions the query (assistant-ui's own search matches descriptions; the composer wraps the adapter's `search` with the same ranking). (f) `AuditLog` queues records made before `bind` (commands run while connecting) and stamps them once the session exists. (g) `/priors/index.md` is authored per case (design 04 §4) and served verbatim; `meta.json.study.date` is the current study's date. (h) Dev: the dev server must run **outside** the sandbox for Chrome to reach it; the Chrome tool must be pointed at the local browser; `lb key run openai-personal -- just dev` (no `--env`); `just smoke` starts its own bridge — stop `just dev` first or run `node apps/bridge/scripts/smoke.ts` against the running one.
- Bridge framing: one JSON-RPC message per WebSocket frame ⇄ one NDJSON line on the agent's stdio. The bridge never parses ACP.
- `_meta.rad` is the only extension slot on `initialize`, `session/new`, `session/prompt` (ACP v1; no root fields).
- Python SDK (`agent-client-protocol` 0.12.1) **spreads `_meta` contents into handler kwargs** (`acp.utils.model_to_kwargs`): `_meta.rad` arrives as `kwargs["rad"]`, not `field_meta`. Responses carry `_meta` via `field_meta=`.
- `initialize.result._meta.rad.model` is informational (display/audit); the agent reports its `RAD_MODEL`.
- `deepagents-acp` calls the agent factory lazily at the first prompt, not at `session/new`; `session/new` returns no `modes`, so the editor's `set_mode` hygiene is a no-op for it and only fires for registry agents.
- **Level 0 agents (spike 1b; target `claude-agent-acp` first, Gemini second):** a registry agent's permission gating is governed by the *host user's* settings and the model's tool choice, not by the Client. The Client therefore (a) pins `session/set_mode: default` after `session/new` when `modes` is advertised — *built in slice 3*, (b) filters `allow_always` from write permissions (INV-1) — *built*, (c) ignores their `available_commands_update` (host-skill leak) — *slice 7*, (d) learns of edits from the on-disk mirror, not `fs/write_text_file` — *slice 7*. For `claude-agent-acp` the edit `diff` arrives in `tool_call_update.content` *before* `session/request_permission` and the two share a `toolCallId`; deepagents-acp does not (see Slice 3 contracts), which is why the editor also correlates by `rawInput`.
- Unknown `sessionUpdate` kinds (`usage_update`, `config_option_update`) pass through the TS SDK; the sidebar must tolerate them.
- **Slice 3:** (a) deepagents-acp sends the edit `diff` on `tool_call` *before* `session/request_permission`, but the permission request carries a **fresh uuid**, not the tool call id — correlate by `rawInput` (`file_path` + `old_string`/`new_string`). (b) `edit_file` never gets a completion update — a resolved permission ends the card. (c) The backend does read-modify-write; while a grant is open the editor serves the proposal's base text for that path (≤ 60 s) so the agent's edit reproduces what it showed; the write is then `applied` (equals the decided buffer) or `partial`, returned in `WriteTextFileResponse._meta.rad`. (d) Clinical verbs (`accept` / `accept_edit` / `reject`) go on the wire by wrapping the ACP client connection (`PermissionRewritingClient`), mapped back to deepagents' `approve`/`reject`. (e) `BRIDGE_TRACE=1` logs frame methods/ids both ways — the bridge stays a pipe otherwise, except `_rad/audit` persistence.
- **Slice 2:** `session/new._meta.rad.manifest: string[]` lists every readable virtual path (ACP v1 has no `ls`); `ls`/`glob` answer from it, `grep` reads candidates. Converter API is `Op[]`-based (no runtime Delta class; `quill-delta` is a type-only dep) so Quill's Delta and the package never need the same class. Absent section ⇒ absent file ⇒ `-32004` (US-WA has no `technique`) — **superseded 2026-08-31**: an absent section reads as `""` and the manifest always lists all five (design 06 §6). Every `fs/write_text_file` → `-32003` until the proposal flow (slice 3). Parse rule: `**` opens bold only before a non-space, closes only after one; `_` only at word boundaries and never adjacent to `_`; an unclosed opener unwinds to literal text.

## Open questions

- (none attached to scheduled work)
